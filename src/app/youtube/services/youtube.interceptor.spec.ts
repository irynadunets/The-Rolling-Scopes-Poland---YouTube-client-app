import { Youtube.Interceptor } from './youtube.interceptor';

describe('Youtube.Interceptor', () => {
  it('should create an instance', () => {
    expect(new Youtube.Interceptor()).toBeTruthy();
  });
});
